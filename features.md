# Pomodoro Timer Web App 機能一覧

## ① タイマーコア機能

| 機能 | 詳細 |
|------|------|
| カウントダウン | 25分（作業）/5分（休憩）のカウントダウン |
| 開始 | タイマーをゼロから開始する |
| リセット | タイマーを初期時間に戻す |
| フェーズ切替 | 作業セッション終了後、自動的に休憩フェーズへ切り替える |
| 完了検知 | タイマーが 0:00 になったことを検知してセッション完了を記録する |

> 開始後にボタンが「一時停止」に変わる設計か要確認。

---

## ② UI表示機能

| 機能 | 詳細 |
|------|------|
| 時間表示 | `mm:ss` 形式でのカウントダウン表示（例: 25:00） |
| フェーズ表示 | 「作業中」「休憩中」のラベル表示 |
| 円形プログレスリング | 残り時間に応じて円弧が減っていくアニメーション |
| ボタン状態の切替 | 「開始」押下後に「一時停止」へ変化するなど、状態に応じたボタン制御 |

---

## ③ 今日の進捗パネル

| 機能 | 詳細 |
|------|------|
| 完了セッション数の表示 | 今日完了したポモドーロ数 |
| 集中時間の表示 | 今日の合計集中時間 |
| 当日リセット | 日付が変わったら進捗をリセットする |

---

## ④ 状態管理

| 機能 | 詳細 |
|------|------|
| タイマー状態の保持 | idle / running / paused を管理 |
| フェーズ状態の保持 | work / break を管理 |
| セッション完了数の集計 | 完了したポモドーロ数を累積カウント |
| 集中時間の集計 | 完了した作業セッションの合計時間を計算 |

---

## ⑤ バックエンド API

| エンドポイント | 機能 |
|------------|------|
| `GET /` | メイン画面の返却 |
| `POST /api/timer/start` | タイマー開始 |
| `POST /api/timer/pause` | 一時停止 |
| `POST /api/timer/resume` | 再開 |
| `POST /api/timer/reset` | リセット |
| `GET /api/timer/status` | 現在の状態取得（ポーリング用） |
| `GET /api/stats` | 今日の進捗データ取得 |

---

## ⑥ フロントエンド

| 機能 | 詳細 |
|------|------|
| 定期ポーリング | `GET /api/timer/status` を一定間隔で呼び出して表示を更新 |
| 円弧アニメーション更新 | SVG または Canvas で進捗リングを描画・更新 |
| ボタン制御 | 状態に応じてボタンのラベルと活性・非活性を切り替える |

---

## ⑦ 通知機能

| 機能 | 詳細 |
|------|------|
| ブラウザ通知 | セッション完了時に Web Notification API で通知する |
| 音声通知 | セッション完了時に通知音を鳴らす |

---

## 実装の優先順位

```
Phase 1（最小動作）:
  タイマーコア + 時間表示 + 開始/リセットボタン

Phase 2（UI完成）:
  円形プログレスリング + フェーズ切替 + ボタン状態制御

Phase 3（進捗機能）:
  完了カウント + 集中時間集計 + 今日の進捗パネル

Phase 4（通知）:
  ブラウザ通知 + 音声通知
```
